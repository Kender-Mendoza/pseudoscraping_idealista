const headers = Array.from(table.querySelectorAll("thead th span")).map(e => e.innerText.trim()), jsonData = Array.from(table.querySelectorAll("tbody tr")).map(e => { let r = Array.from(e.querySelectorAll("td")), t = {}; return r.forEach((e, r) => { t[headers[r]] = e.innerText.trim() }), t }), jsonString = JSON.stringify(jsonData, null, 2), blob = new Blob([jsonString], { type: "application/json" }), url = URL.createObjectURL(blob); window.open(url, "_blank");